character=input("Enter a character: ")
if ord(character)>=97 and ord(character)<=122 or ord(character)>=65 and ord(character)<=90:
    print("{} is a Alphabet".format(character))
elif ord(character)>=48 and ord(character)<=57:
    print("{} is a Digit".format(character))
elif ord(character)==32:
    print("The charcater is a Space")
elif ord(charcter)>=33 and ord(character)<=47 or ord(character)>=91 and ord(character)<=96 or ord(character)>=123 and ord(character)<=126 :
    print("{} is a special character".format(character))
